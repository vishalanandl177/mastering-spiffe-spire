# Lab 1 — Installing SPIRE from Binaries

## Objective

Install and run SPIRE Server and Agent on a single Linux/macOS machine using join token attestation.

## Steps

```bash
./install-spire.sh
./start-server.sh
./register-and-start-agent.sh
```
